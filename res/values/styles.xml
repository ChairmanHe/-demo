<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="couponDialogStyle" parent="@android:Theme.Dialog">

        <!-- 去掉Dialog的标题栏 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 去掉Dialog的黑色背景框 -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="CustomDialogStyle" parent="@android:style/Theme.Translucent.NoTitleBar.Fullscreen">

        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- <item name="android:backgroundDimEnabled">false</item> -->
    </style>
    <!-- 自定义progressbar样式 可以改变进度框的图片   需要定义一个旋转的动画 -->
    <style name="ProgressBar">
        <item name="android:indeterminateDrawable">@anim/progressbar_style</item>
    </style>

</resources>